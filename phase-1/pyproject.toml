[project]
name = "todo-app"
version = "0.1.0"
description = "In-memory console-based todo application (Phase I)"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
